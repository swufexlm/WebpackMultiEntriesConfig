import * as ReactDOM from 'react-dom';
import * as React from 'react';
import But from 'src/components/index';
const styles = require('./index.less');
styles['2'] = '2';

ReactDOM.render(<But />, document.getElementById('root'));
